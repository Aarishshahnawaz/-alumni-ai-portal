# 🔴 URGENT: Gmail App Password Configuration Required

## Current Issue
Your OTP registration system is failing with **500 Internal Server Error** because the Gmail App Password is not configured.

## Current Status in `.env` file:
```
EMAIL_USER=iitianaarish@gmail.com  ✅ CORRECT
EMAIL_PASS=PASTE_YOUR_16_DIGIT_APP_PASSWORD_HERE  ❌ PLACEHOLDER - NEEDS REAL PASSWORD
```

---

## 🎯 How to Generate Gmail App Password

### Step 1: Enable 2-Step Verification
1. Go to: https://myaccount.google.com/security
2. Find "2-Step Verification" section
3. Click "Get Started" and follow the setup process
4. **You MUST enable 2-Step Verification before you can create App Passwords**

### Step 2: Generate App Password
1. Go to: https://myaccount.google.com/apppasswords
2. You may need to sign in again
3. In the "Select app" dropdown, choose "Mail"
4. In the "Select device" dropdown, choose "Other (Custom name)"
5. Type: "AlumniAI Portal Backend"
6. Click "Generate"
7. **Google will show you a 16-digit password** (example: `abcd efgh ijkl mnop`)

### Step 3: Copy the App Password
- Copy the 16-digit password (it will have spaces)
- **REMOVE ALL SPACES** when pasting into `.env`
- Example: `abcd efgh ijkl mnop` becomes `abcdefghijklmnop`

---

## 📝 Update Your `.env` File

Open: `alumni-portal-backend/.env`

Find line 29 and replace:
```env
EMAIL_PASS=PASTE_YOUR_16_DIGIT_APP_PASSWORD_HERE
```

With your actual App Password (no spaces):
```env
EMAIL_PASS=abcdefghijklmnop
```

---

## 🚀 Restart Backend Server

After updating `.env`:

1. Stop the backend server (Ctrl+C)
2. Start it again:
   ```bash
   cd alumni-portal-backend
   npm start
   ```

3. You should see in console:
   ```
   🔍 Environment Variables Check:
   📧 EMAIL_USER: iitianaarish@gmail.com
   🔑 EMAIL_PASS configured: true
   ```

---

## ✅ Test OTP System

### Option 1: Test Email Endpoint
```bash
curl http://localhost:5000/api/test/test-email
```

Should return:
```json
{
  "success": true,
  "message": "Test email sent successfully"
}
```

### Option 2: Test Full OTP Flow
1. Open frontend: http://localhost:3000/register
2. Enter email: `test@example.com` (any email works in dev mode)
3. Select role: Student or Alumni
4. Click "Send OTP"
5. Check your email inbox (and spam folder)
6. You should receive OTP email within seconds

---

## 🔒 Security Notes

1. **App Password is NOT your Gmail password**
   - It's a special 16-digit code generated by Google
   - Only works for this specific application
   - Can be revoked anytime from Google Account settings

2. **Never share your App Password**
   - Don't commit `.env` file to Git
   - `.env` is already in `.gitignore`

3. **If App Password stops working:**
   - Revoke old App Password in Google Account
   - Generate new App Password
   - Update `.env` file
   - Restart server

---

## 🐛 Common Errors

### Error: "Gmail authentication failed"
- **Cause**: Wrong EMAIL_PASS or 2-Step Verification not enabled
- **Fix**: Generate new App Password, ensure 2-Step Verification is ON

### Error: "Invalid credentials"
- **Cause**: Using normal Gmail password instead of App Password
- **Fix**: Must use 16-digit App Password, not regular password

### Error: "Less secure app access"
- **Cause**: Old Gmail security setting (deprecated)
- **Fix**: Use App Password method (current standard)

---

## 📧 Email Configuration Summary

| Setting | Value | Status |
|---------|-------|--------|
| EMAIL_USER | iitianaarish@gmail.com | ✅ Configured |
| EMAIL_PASS | PASTE_YOUR_16_DIGIT... | ❌ Needs Update |
| EMAIL_HOST | smtp.gmail.com | ✅ Correct |
| EMAIL_PORT | 587 | ✅ Correct |

---

## 🎯 After Configuration

Once configured, your system will:
- ✅ Send real OTP emails via Gmail SMTP
- ✅ Support Gmail/Yahoo in development mode (for testing)
- ✅ Block public emails in production mode
- ✅ Verify OTP before user registration
- ✅ No OTP required for login (only registration)

---

## 📞 Need Help?

If you're still facing issues after following these steps:
1. Check backend console for detailed error messages
2. Verify 2-Step Verification is enabled
3. Try generating a new App Password
4. Ensure no spaces in EMAIL_PASS value
5. Restart backend server after changes
